name: earnings_surprise_tracker
version: '1.0.0'
config-version: 2

profile: earnings_surprise_tracker

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  earnings_surprise_tracker:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    analytics:
      +materialized: table
      +schema: analytics